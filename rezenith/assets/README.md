# /assets

Place your static assets here.

Examples:
- `logo.png`       — ReZenith logo
- `favicon.ico`    — Browser tab icon
- `bg.jpg`         — Background image for welcome cards
- `fonts/`         — Custom font files
