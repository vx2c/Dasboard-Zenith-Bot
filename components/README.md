# /components

Place reusable JavaScript or HTML component files here.

Examples:
- `particles.js`   — Custom particle effects
- `chart.js`       — Custom chart components
- `embed-builder.js` — Standalone embed builder widget
